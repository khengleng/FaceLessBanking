{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "strategy": "rolling"
  },
  "notes": "Template placeholder. Create real Railway project/service mappings in platform UI or generated config."
}
