# ekyc-orchestration environment example
# Required: set these for every runtime environment.
SERVICE_NAME=ekyc-orchestration
NODE_ENV=development
PORT=3000

# Optional defaults (recommended for consistency).
HOST=0.0.0.0
LOG_LEVEL=info

# Required for webhook signature verification.
SUMSUB_WEBHOOK_SECRET=change-me-in-runtime
