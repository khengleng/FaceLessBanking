# fee-charge-engine environment example
# Required: set these for every runtime environment.
SERVICE_NAME=fee-charge-engine
NODE_ENV=development
PORT=3000

# Optional defaults (recommended for consistency).
HOST=0.0.0.0
LOG_LEVEL=info

# Optional in local dev (default exists), required for shared/staging/prod.
KAFKA_BROKERS=localhost:9092
