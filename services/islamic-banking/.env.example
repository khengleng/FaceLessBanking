# islamic-banking environment example
# Required: set these for every runtime environment.
SERVICE_NAME=islamic-banking
NODE_ENV=development
PORT=3000

# Optional defaults (recommended for consistency).
HOST=0.0.0.0
LOG_LEVEL=info
