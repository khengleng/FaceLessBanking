# workflow-case-management environment example
# Required: set these for every runtime environment.
SERVICE_NAME=workflow-case-management
NODE_ENV=development
PORT=3000

# Optional defaults (recommended for consistency).
HOST=0.0.0.0
LOG_LEVEL=info
