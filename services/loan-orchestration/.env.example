# loan-orchestration environment example
# Required: set these for every runtime environment.
SERVICE_NAME=loan-orchestration
NODE_ENV=development
PORT=3000

# Optional defaults (recommended for consistency).
HOST=0.0.0.0
LOG_LEVEL=info

# Optional (default exists), set explicitly for deterministic deployments.
LOAN_FUNDING_ACCOUNT_ID=loan-funding-account
